# Agent Instructions

<!--
PLACEHOLDER: shared instructions for all AI coding agents go here.
This file is the single source of truth:
  - Claude Code reads it via the @AGENTS.md import in claude/CLAUDE.md
    (install.sh symlinks it to ~/.claude/AGENTS.md).
  - Codex reads it natively (install.sh symlinks it to ~/.codex/AGENTS.md).
Put agent-specific overrides in that agent's own file, not here.
-->
